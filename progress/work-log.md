# 개발 내역

# 9월
## 2026-09-02

| 개발 작업 | 발생했던 문제 상황 | 적용한 해결 방안 (1줄 요약) |
|---|---|---|
| **대용량 파일 검색 도구 최적화** | 수십 개 이상의 검색 결과 텍스트가 한 번에 유입되어 LLM 컨텍스트 과부하 및 응답 지연 발생 | 검색 결과 원본은 로컬 캐시(`.search_cache.json`)에 격리 저장하고 LLM에는 개수 메타데이터만 반환하도록 경량화 |
| **검색 결과 텍스트 파일 저장 파이프라인** | LLM이 프롬프트 요약본만 기억하여 파일 저장 시 전체가 아닌 일부 목록만 기록됨 | 백엔드가 캐시 파일에서 원본 데이터를 직접 꺼내 파일로 일괄 출력하는 전용 저장 도구 구축 |
| **도구 중복 호출 및 파일 덮어쓰기 방어** | LLM의 사전 학습된 '파일 쓰기 반사'로 인해 저장 도구 호출 직후 `write_file`로 덮어쓰는 사고 발생 | 도구 스키마 및 프롬프트로 도구의 데이터 출처와 역할을 분리하고, 코드 레벨에서 연속 동일 경로 덮어쓰기 차단 |
| **파일명 충돌 방지 로직 구축** | 동일한 파일명이 이미 존재하는 경우 사용자 확인 없이 기존 파일 내용이 덮어씌워짐 | 파일 존재 시 `(1)`, `(2)` 넘버링을 자동으로 덧붙여 고유 경로를 생성하는 유틸리티 함수(`get_unique_filepath`) 적용 |
| **검색 결과 내 가짜 파일명 생성 방지** | 프롬프트가 예시 파일명을 요구했으나 전달된 데이터가 없어 LLM이 가짜 파일명을 임의로 창작함 | 샘플 추출 코드를 완전히 덜어내고 프롬프트를 '발견 개수 보고 및 다음 행동 제안'에만 집중하도록 수정 |
| **용량 범위 조건 검색 지원** | '1GB 이상 2GB 이하'처럼 상한선이 있는 범위를 요구했을 때 최소 용량으로만 검색됨 | `search_files` 스키마에 `max_size_mb` 파라미터를 추가 등록하여 LLM이 상한선 조건을 파이썬 엔진에 전달하도록 개선 |
| **캐시 기반 부분 탐색 및 선별 저장** | 검색 완료 후 특정 파일 존재 여부 질의나 키워드 선별 저장 시 디스크 전체를 다시 훑어야 하는 비효율 발생 | 디스크 재탐색 없이 캐시 내에서 키워드를 고속 판별하는 `check_in_last_search` 추가 및 저장 도구에 `keyword` 필터 파라미터 확장 |

## 2026-09-03

### 작업 결과

| 구분 | 작업 내용 | 주요 변경 및 구현 상세 | 결과 및 상태 |
| --- | --- | --- | --- |
| **디버깅 & 최적화** | 드라이브 루트 경로 정규식 보정 | `D:` 형태의 입력이 작업 디렉터리로 잘못 인식되는 현상을 막기 위해 정규식(`re.match`)으로 `D:\\` 루트 자동 변환 처리 | 대용량 드라이브 루트 검색 정상 진입 보장 |
| **안전장치 검증** | 멀티턴 도구 루프 동작 확인 | 5만 개 한도(`MAX_SCANNED_LIMIT`) 도달 시 에이전트가 멈추지 않고 스스로 하위 경로를 좁혀 재시도하는 자율 추론 흐름 검증 | 프로세스 프리징 없이 대규모 탐색 수행 |
| **구조 개선** | 프롬프트 3단계 모듈화 및 템플릿화 | 기존 단일 프롬프트를 `01_persona.md`, `02_general.md`, `03_files.md`로 물리적 분할, `.example` 템플릿 격리 및 `.gitignore` 등록 | 개인 설정 보호 및 프롬프트 유지보수성 확보 |
| **신규 도구 구현** | 경량 폴더 전용 도구 `search_folders` 추가 | 파일 검사(`os.stat`)를 제외하고 디렉터리 트리만 초고속 수집하는 전용 함수 및 스키마 추가, 동사 일관성(`search_*`) 확보 | 디스크 지도 초고속 파악 및 계층 탐색 기반 마련 |
| **추론 체계 고도화** | 계층적 탐색(Tree-of-Thought) 전략 수립 | `prompts/03_files.md`에 [1단계 상위 폴더 파악 → 2단계 유력 폴더 정밀 검색 → 3단계 전수 조사] 3단계 추론 규칙 주입 | 무작정 루트 전체를 재귀 탐색하던 비효율 제거 |
| **프롬프트 튜닝** | 도구 호출 최적화 및 낭비 제거 | 대소문자 자동 무관 안내, 동일 인자 중복 호출 금지, 불필요한 전체 파일 덤프/읽기 금지 수칙 명시 | 도구 호출 단계 압축 (7턴 → 2턴 최단 경로 탐색 성공) |
| **CLI 제어 구현** | 런타임 모델 동적 스위칭 (`/model`) 추가 | 터미널 대화 루프 진입 전 입력을 가로채는 핸들러 구현, `/model <alias>`, `/clear` 등 시스템 제어 명령 분기 처리 | 대화 히스토리를 유지한 채 무중단 모델 스위칭 지원 |
| **버그 해결** | `KeyError: 'model_name'` 예외 처리 | `MODEL_PROFILES`의 딕셔너리 키(`name`)와 출력 함수의 참조 키 불일치 문제를 `.get("name")` 안전 접근 방식으로 통일하여 수정 | 프로필 조회 및 전환 오류 해결 |
| **설정 보강** | 모델 프로필 가독성 개선 | `config/settings.py` 내 각 모델별 `"description"` 필드를 채워 `/model` 목록 호출 시 직관적인 한 줄 안내 표시 | 모델별 용도 및 상태 파악 용이 |
| **설계 결정** | 다국어/이명 키워드 가이드 및 체급 정비 유보 | 1차 검색 실패 시 사용자 역제안이 이미 작동 중임을 감안하여 자동 이명 추론은 추후로 보류, 32B의 VRAM 부담에 따라 7B~14B 중심 경량 조합 테스트로 방향 정립 | 과도한 프롬프트 강제 방지 및 로컬 자원 사용성 최적화 |
| **아키텍처 분석** | LangChain 도입 타당성 검토 | 직접 구현한 파이썬 구조와 LangChain 프레임워크(BaseTool, ReAct 런타임 등) 전환 절차 및 득실 비교 | 현 경량 시스템에서는 직접 제어 구조 유지가 유리함을 확인, 차후 RAG 도입 시 검토하기로 정리 |
| **의도 분류 설계** | 잡담 vs 작업 구분 및 과잉 도구 호출 방지책 수립 | "커피 마시고 싶다" 등 감정/상태 표현 시 도구를 호출하는 오작동(Over-tooling) 방지 방안 분석 | 행동 동사 기반 프롬프트 분기 및 RP 로어북(키워드 트리거/상태창) 메커니즘 대입 설계 완료 |

### 남은 투두리스트

| 우선순위 | 작업 항목 | 세부 내용 및 구현 방향 |
| :--- | :--- | :--- |
| **높음** | **로어북 방식 조건부 도구 활성화 (Dynamic Tool Exposure)** | - 입력 텍스트 내 특정 키워드/행동 동사/마커 감지 시에만 API 호출에 `tools` 스키마를 동적으로 바인딩<br>- 일상 잡담 시 도구 스키마를 완전히 배제하여 과잉 호출 원천 차단 및 토큰 절약 |
| **중간** | **경량 모델 라인업 및 런타임 옵션 점검** | - 32B의 VRAM 부담을 대체할 쾌적한 경량 모델(7B~14B 계열) 탐색 및 프로필 등록<br>- `/model` 전환 시 `num_ctx`, `temperature` 등 프로필 옵션 누락 여부 점검 |
| **추후** | **다국어/이명(Alias) 키워드 추론 가이드 고도화** | - 실사용 데이터 누적 후 한글 음차 ↔ 원어 ↔ 영어 표기 불일치에 대한 1회 자동 재시도 프롬프트 정밀화 |
| **장기** | **대규모 확장 시 프레임워크 전환 (LangChain/RAG)** | - 로컬 대용량 문서 색인/임베딩(RAG)이나 복잡한 멀티 에이전트 확장이 필요해질 때 랭체인 전환 검토 |

## 2026-09-07

### 작업 결과

| 구분 | 작업 내용 | 상세 설명 |
| :--- | :--- | :--- |
| **모듈 연동** | `tools/__init__.py` 동적 실행기 연결 | `ALL_SCHEMAS`와 `execute_tool` 함수를 활용하도록 `engine.py` 인터페이스 정비 |
| **엔진 복원** | `ollama` 기반 네이티브 런타임 1:1 이식 | `openai` 클라이언트를 걷어내고 원본 `agent.py`의 `ollama.chat` 호출 구조 복구[cite: 2, 4] |
| **후처리 파이프라인** | `<think>` 사고 과정 정제 로직 유지 | 정규식을 통해 추론 모델의 생각 태그 찌꺼기를 걸러내고 UI에 깨끗한 본문만 전달[cite: 2, 4] |
| **UI 상호작용 제어** | 추론 중 컴포넌트 락(`disabled`) 처리 | 모델 답변 생성 중 입력창 및 사이드바 제어 요소 비활성화 (마우스 호버 시 금지 커서 노출) |
| **안전장치 UI** | 대화 내역 초기화 모달 적용 | `@st.dialog`를 활용해 실수 방지용 [삭제 확인 / 취소] 팝업 레이어 구성 |
| :--- | :--- | :--- |
| **엔진 리팩토링** | `engine.py` 동적 파라미터 주입 인터페이스 전환 | 하드코딩된 `mode` 문자열 분기 대신 `prompt_files`, `tools` 목록을 직접 주입받아 유연하게 추론하는 구조로 변경 |
| **자원 관리 모듈** | `room_manager.py` 신설 | `prompts/` 내 마크다운 파일 자동 스캔 및 도구 스키마/기본 룸 템플릿 반환 체계 구축 |
| **UI 동적 룸 전환** | `app.py` 세션 딕셔너리 구조(`rooms`) 전면 개편 | 하드코딩 방식을 탈피하고 `@st.dialog` 기반 방 생성 모달 및 사이드바 동적 라디오 연동 구현 |
| **신규 도구 구현** | `tools/chat_utils.py` 신설 | 잡담방 템플릿 안정화 및 실시간 상호작용을 위한 시간(`get_current_time`) 및 날씨(`get_current_weather`) 도구 모듈 제작 |
| **도구 디스패처 확장** | `tools/__init__.py` 동적 모듈 순회 연동 | `chat_utils` 모듈을 `TOOL_MODULES` 및 `ALL_SCHEMAS`에 등록하고 키워드 언패킹(`**args`) 지원 |
| **의존성 최적화** | 미사용 패키지 제거 및 코드 정리 | `openai` 라이브러리 언인스톨 및 `app.py` 내 세션 동기화 로직 간소화 |

### 개념 정리 및 투두리스트

| 분류 | 항목 | 세부 내용 |
| :--- | :--- | :--- |
| **개념 정리** | LLM 생성 중단(Interrupt) 메커니즘 | 모델 연산을 강제로 끊는 것이 아니라 데이터 소켓을 닫아 엔진 스스로 중단하도록 유도 |
| **개념 정리** | 비스트리밍 에이전트 인터럽트 설계 | 토큰 단위 텍스트 중단 대신, 위험 방지를 위해 **'도구 실행 직전 승인(Human-in-the-Loop)'** 및 **'도구 턴 단위 중단'** 적용 |
| **개념 정리** | 인터럽트 시 가비지/컨텍스트 관리 | GPU/VRAM에는 누수가 없으나, 미완성 도구 호출 파편이 남지 않도록 해당 턴을 원자적(All-or-Nothing)으로 취소 처리해야 함 |
| **To-Do** | 도구 실행 전 확인(승인) UI 구현 | 파일 삭제, 덮어쓰기 등 로컬 PC 변경 작업 전 사용자 승인을 받는 인터랙션 추가 |
| **To-Do** | 도구 실행 에러 핸들링 고도화 | 파일 권한 부족, 경로 미존재 등 도구 실패 시 모델이 능동적으로 대안 경로를 탐색하도록 보강 |
| **To-Do** | 다중 도구 확장 | `tools/` 패키지에 OS 제어, 웹 검색 등 신규 모듈 등록 및 `ALL_SCHEMAS` 확장 |

## 2026-09-08

### 작업 결과

| 구분 | 작업 내용 | 상세 설명 |
| :--- | :--- | :--- |
| **응답 정제 강화** | `clean_model_output` 자가 반복 문단 필터링 | 모델이 단일 턴 내에서 가상 역할을 만들어 중복 문단을 연달아 출력하는 현상 차단 |
| **도구 표시명 체계** | `TOOL_DISPLAY_MAP` 도입 및 스키마 분리 | LLM용 고유 함수명과 별개로 UI 전용 직관적 타이틀/설명 매핑 구축 |
| **방 생성 유효성 검증** | 3종 필수 검증 및 기본값 세팅 | 방 이름 공백 방지, 프롬프트(페르소나) 및 도구(시간 확인) 필수 선택 보장 |
| **헤더 인터페이스 간소화** | `st.popover` 기반 방 설정 상세화 | 상시 노출되던 메타 텍스트를 숨기고 팝오버를 통해 장착 프롬프트/도구 목록 열람 분리 |
| **방 관리 및 예외 처리** | 채팅방 완전 삭제 및 Empty State 대응 | 방 삭제 다이얼로그 추가 및 모든 방 삭제 시 플레이스홀더 안내(`st.stop()`) 처리 |
| **모달 레이아웃 버그 수정** | 컬럼 렌더링 유지 구조 개선 | 유효성 검증 실패 시 조기 `return`을 `if/elif/else`로 변경하여 에러 출력 상태에서도 `col2`가 정상 렌더링되도록 수정 |

## 2026-09-13

### 작업 결과

| 구분 | 작업 내용 | 상세 설명 |
| :--- | :--- | :--- |
| **도구 안정성 (`tools/files.py`)** | 파일 내보내기 경로 격리 및 스키마 보정 | `export_search_results_to_file`의 저장 경로를 프로젝트 루트의 `search_result` 폴더로 강제 귀속하고, `FILES_SCHEMAS`의 `dest_path` 필수 제약을 해제하여 안전성 확보 |
| **프롬프트 모듈화 (`prompts/`)** | 도구 스펙과 제어 워크플로우 분리 | `03_files.md`는 순수 도구 API 스펙으로 경량화하고, `04_agent_workflow.md`를 신설하여 탐색 전략(ToT), 종료 조건, UX 보고 규칙을 전담하도록 관심사 분리 |
| **세션 메모리 구현 (`engine.py`, `app.py`, `room_manager.py`)** | 세션별 `context` 구조 구축 및 동적 주입 | 방 객체에 `cwd`, `last_keyword` 슬롯을 추가하고 도구 실행 시 경로를 자동 갱신한 뒤, 매 추론 전 시스템 프롬프트에 현재 작업 상태를 동적 주입하여 지칭 대명사 탐색 지원 |

## 2026-09-15

### 작업 결과

| 구분 | 작업 내용 | 상세 설명 |
| :--- | :--- | :--- |
| **도구 구현** | 크롬 방문 기록 조회 도구 신설 (`tools/browser.py`) | 로컬 SQLite 기반 Chrome `History` 파일 복제 및 조회 로직 구현. 키워드, 기간(`days`), 건수(`limit`) 필터링을 지원하고 LLM이 읽기 쉬운 압축 텍스트로 반환하도록 포매팅 적용 |
| **모듈 등록** | 브라우저 도구 스키마 및 디스패처 연동 | `BROWSER_SCHEMAS` 정의 및 `tools/__init__.py` 등록을 통해 기존 파일 제어 도구들과 함께 단일 모델 컨텍스트에서 정상 호출되도록 구성 |
| **리팩토링** | `engine.py` 모듈화 및 서브루틴 분리 | 단일 책임 원칙(SRP)에 맞춰 폴백 JSON 파싱(`_parse_fallback_tool_call`), 빈 응답 제어(`_handle_empty_response`), 결과 직렬화(`_serialize_tool_result`)를 헬퍼 함수로 추출하여 메인 루프 가독성 개선 |
| **의존성** | 랭그래프 마이그레이션 패키지 추가 | `requirements.txt`에 `langgraph`, `langchain-core`, `langchain-ollama`, `langgraph-checkpoint-sqlite` 추가 및 환경 설치 완료 |
| **아키텍처 검증** | 랭그래프 최소 단위 파이프라인 검증 (`test_graph.py`) | `StateGraph`, 노드(`chatbot`, `summarizer`), 조건부 엣지(`should_summarize`)를 연결하여 메시지 수 기반 분기 동작 및 대화 요약 갱신 파이프라인 검증 |