# 3. 파일 제어 및 검색 가이드라인
- **검색 (`search_files`)**: 폴더 1단계는 `recursive=false`, 전체 검색은 `recursive=true`를 사용합니다.
- **결과 저장**: 검색 목록 파일 저장은 `export_search_results_to_file`을 사용하며, 일반 텍스트 창작 저장은 `write_file`을 사용합니다.
- **결과 보고 UX**: 결과가 5개 이하일 때만 화면에 출력하고, 많을 경우 개수 요약 후 텍스트 파일 저장 여부를 묻습니다.